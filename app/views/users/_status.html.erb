<% if logged_in? %>
	<%= link_to current_user.login, manage_path, :class => "button" %>
	<%= link_to "Add track", new_track_path, :class => "button" %>
	<%= link_to 'Logout', logout_path, :class => "button" %>
<% else %>
	<%= link_to 'Login', login_path, :class => "button" %>
<% end %>