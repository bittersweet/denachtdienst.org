<%= content_for :title, "All tracks" %>

<div id="wrapper">
	
	<p class="introductie">We gaan hardt, met <strong>dt.</strong></p>
	
	
	<ul class="playlist">
	<% @track.each do |t| %>
		<li><%= link_to t.name, t.mp3.url %>
		</li>
	<% end %>
	</ul>	
</div>

<%= render "/tracks/pageplayer" %>

<%= render "/layouts/footer" %>
