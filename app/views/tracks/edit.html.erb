<div class="standard_form">
<% form_for(@track) do |f| %>

	<div class="formerror"><%= error_message_on "track", "name" %></div>

	<%= f.label :name, "Track name" %>
	<%= f.text_field :name %>
	
	<%= f.submit "Update" %>

<% end %>
</div>
